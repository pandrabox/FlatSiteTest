---
// src/pages/guide/index.astro
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';

const currentBase = import.meta.env.BASE_URL;

// Get all guides and sort by stepNumber
const allGuides = await getCollection('guides');
const sortedGuides = allGuides.sort((a, b) => a.data.stepNumber - b.data.stepNumber);

const SEO_TITLE = "VRChatケモノ初心者入門ガイド | VRで始めるもふもふライフの歩き方";
const SEO_DESCRIPTION = "「VRChatでケモノになってみたい！」そんな初心者の夢を叶える7つのステップ。アバター選び、無料試着、コミュニティ交流、Booth購入、Unityアップロード、着せ替え改変まで徹底解説！";
---

<!DOCTYPE html>
<html lang="ja">
  <head>
    <BaseHead title={SEO_TITLE} description={SEO_DESCRIPTION} />
    <style>
      .guide-header {
        text-align: center;
        background: linear-gradient(135deg, hsl(37, 95%, 72%) 0%, hsl(30, 85%, 45%) 100%);
        color: var(--color-text-white);
        padding: 5rem 1.5rem;
        border-radius: 0 0 var(--radius-lg) var(--radius-lg);
      }

      .guide-header h1 {
        font-size: clamp(2rem, 5vw, 3.2rem);
        color: var(--color-text-white);
        margin-bottom: 1rem;
        font-weight: 900;
        text-shadow: 0 4px 10px rgba(0,0,0,0.1);
      }

      .guide-header p {
        font-size: clamp(0.95rem, 2vw, 1.15rem);
        color: rgba(255, 255, 255, 0.9);
        max-width: 700px;
        margin: 0 auto;
        line-height: 1.6;
      }

      .guide-container {
        max-width: 900px;
        margin: 4rem auto;
        padding: 0 1.5rem;
        position: relative;
      }

      /* 縦のタイムライン軸線 */
      .timeline-track {
        position: absolute;
        top: 0;
        bottom: 0;
        left: 40px;
        width: 4px;
        background: linear-gradient(to bottom, var(--color-brand) 0%, var(--color-brand-light) 70%, rgba(244, 173, 57, 0.1) 100%);
        border-radius: 2px;
        z-index: 1;
      }

      .step-item {
        position: relative;
        z-index: 2;
        margin-bottom: 4rem;
        padding-left: 80px;
        display: flex;
        flex-direction: column;
      }

      .step-badge {
        position: absolute;
        left: 12px;
        top: 0;
        width: 60px;
        height: 60px;
        background: var(--color-brand);
        color: var(--color-text-white);
        border-radius: 50%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        font-family: 'Fredoka', sans-serif;
        box-shadow: 0 8px 20px rgba(244, 173, 57, 0.35);
        border: 4px solid var(--color-bg);
        transition: var(--transition-smooth);
      }

      .step-badge-num {
        font-size: 1.4rem;
        font-weight: 800;
        line-height: 1;
      }

      .step-badge-label {
        font-size: 0.55rem;
        font-weight: 700;
        letter-spacing: 0.05em;
      }

      .step-item:hover .step-badge {
        transform: scale(1.1) rotate(15deg);
        background: var(--color-accent);
        box-shadow: 0 8px 20px rgba(231, 76, 60, 0.35);
      }

      .step-card {
        background: var(--color-bg-glass);
        border: 1px solid var(--color-border);
        border-radius: var(--radius-lg);
        padding: 2rem;
        box-shadow: var(--shadow-glass);
        transition: var(--transition-smooth);
      }

      .step-card:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-medium);
        border-color: var(--color-brand);
      }

      .step-card h2 {
        font-size: 1.4rem;
        font-weight: 800;
        color: var(--color-text);
        margin-bottom: 1rem;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .step-card p {
        color: var(--color-text-muted);
        font-size: 0.95rem;
        line-height: 1.7;
        margin-bottom: 1.5rem;
      }

      .step-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 1rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(244, 173, 57, 0.1);
      }

      .keyword-tag {
        background-color: var(--color-bg-secondary);
        color: var(--color-text-muted);
        font-size: 0.75rem;
        padding: 4px 12px;
        border-radius: 20px;
        border: 1px solid rgba(0,0,0,0.05);
      }

      @media (max-width: 600px) {
        .timeline-track {
          left: 20px;
        }

        .step-item {
          padding-left: 50px;
        }

        .step-badge {
          width: 44px;
          height: 44px;
          left: -2px;
        }

        .step-badge-num {
          font-size: 1.1rem;
        }

        .step-badge-label {
          display: none;
        }

        .step-card {
          padding: 1.25rem;
        }

        .step-card h2 {
          font-size: 1.15rem;
        }
      }
    </style>
  </head>
  <body>
    <Header />

    <!-- ページヘッダー -->
    <header class="guide-header">
      <h1>VRChatケモノ初心者入門ガイド</h1>
      <p>「VRでケモノになって、もふもふに囲まれて暮らしたい！」<br/>そんなあなたの夢を、アバターの選び方からカスタムまで全7つのステップで丁寧にサポートします。</p>
    </header>

    <!-- ガイド一覧 (タイムライン形式) -->
    <main class="guide-container">
      <div class="timeline-track"></div>

      {sortedGuides.map((guide) => (
        <article class="step-item">
          <div class="step-badge">
            <span class="step-badge-num">{guide.data.stepNumber}</span>
            <span class="step-badge-label">STEP</span>
          </div>

          <div class="step-card">
            <h2>
              <span>🐾</span> {guide.data.title}
            </h2>
            <p>{guide.data.description}</p>
            
            <a href={`${currentBase}guide/${guide.id}/`} class="btn btn-primary btn-sm">
              このステップの解説を見る 🔍
            </a>

            {guide.data.keywords && (
              <div class="step-meta">
                {guide.data.keywords.map((kw) => (
                  <span class="keyword-tag">#{kw}</span>
                ))}
              </div>
            )}
          </div>
        </article>
      ))}
    </main>

    <Footer />
  </body>
</html>
